import { revalidatePath } from "next/cache";
import PageHeader from "@/components/PageHeader";
import { Button, Card, Input, Label, Select, Textarea } from "@/components/Card";
import { prisma } from "@/lib/prisma";
import { money, optionalString, toDecimal } from "@/lib/utils";

async function createSupplier(formData: FormData) { "use server"; await prisma.supplier.create({ data: { name: String(formData.get("name") || ""), supplierType: optionalString(formData.get("supplierType")), licenseNumber: optionalString(formData.get("licenseNumber")), mobile: optionalString(formData.get("mobile")), email: optionalString(formData.get("email")), trnNumber: optionalString(formData.get("trnNumber")), vatRegistered: formData.get("vatRegistered") === "on", paymentTerms: optionalString(formData.get("paymentTerms")), openingBalance: toDecimal(formData.get("openingBalance")), address: optionalString(formData.get("address")), contactPerson: optionalString(formData.get("contactPerson")), bankDetails: optionalString(formData.get("bankDetails")) } }); revalidatePath("/suppliers"); }
async function disableSupplier(formData: FormData) { "use server"; await prisma.supplier.update({ where: { id: String(formData.get("id")) }, data: { isActive: false } }); revalidatePath("/suppliers"); }

export default async function SuppliersPage() { const suppliers = await prisma.supplier.findMany({ orderBy: { createdAt: "desc" } }); return <><PageHeader title="Suppliers" subtitle="Material suppliers, subcontractors and service providers." /><div className="space-y-6 p-8"><Card><form action={createSupplier} className="grid gap-4 md:grid-cols-4"><div><Label>Name</Label><Input name="name" required /></div><div><Label>Type</Label><Select name="supplierType"><option>Material Supplier</option><option>Subcontractor</option><option>Service Provider</option></Select></div><div><Label>License Number</Label><Input name="licenseNumber" /></div><div><Label>Contact Person</Label><Input name="contactPerson" /></div><div><Label>Mobile</Label><Input name="mobile" /></div><div><Label>Email</Label><Input name="email" /></div><div><Label>TRN Number</Label><Input name="trnNumber" /></div><div><Label>Payment Terms</Label><Input name="paymentTerms" /></div><div><Label>Opening Balance</Label><Input name="openingBalance" type="number" step="0.01" defaultValue="0" /></div><div className="flex items-end gap-2"><input name="vatRegistered" type="checkbox" /><span className="text-sm">VAT Registered</span></div><div className="md:col-span-2"><Label>Bank Details</Label><Input name="bankDetails" /></div><div className="md:col-span-4"><Label>Address</Label><Textarea name="address" /></div><div className="md:col-span-4"><Button type="submit">Add Supplier</Button></div></form></Card><Card><table className="w-full text-sm"><thead><tr className="border-b text-left text-slate-500"><th className="py-2">Name</th><th>Type</th><th>Mobile</th><th>Email</th><th>TRN</th><th className="text-right">Opening</th><th>Status</th><th></th></tr></thead><tbody>{suppliers.map((s) => <tr key={s.id} className="border-b last:border-0"><td className="py-2 font-semibold">{s.name}</td><td>{s.supplierType}</td><td>{s.mobile}</td><td>{s.email}</td><td>{s.trnNumber}</td><td className="text-right">{money(s.openingBalance)}</td><td>{s.isActive ? "Active" : "Inactive"}</td><td className="text-right"><form action={disableSupplier}><input type="hidden" name="id" value={s.id} /><button className="text-xs text-red-600">Disable</button></form></td></tr>)}</tbody></table></Card></div></>; }

